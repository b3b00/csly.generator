namespace csly.models;

    public enum GenericToken
    {
        Default,
        Identifier,
        Int,
        Hexa,
        Double,
        Date,
        KeyWord,
        String,
        Char,
        SugarToken,

        Extension,

        Comment,
        UpTo
    }
